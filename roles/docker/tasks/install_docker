---
- name: Install docker repo
  file: src=roles/docker/files/docker.repo path=/etc/yum.repos.d/docker.repo state=file owner=root group=root mode=0644

- name: Install docker
  yum: name=docker-engine state=installed
  notify: start_docker
